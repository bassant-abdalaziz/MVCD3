@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model Department
<h1>Add Department</h1>

@using(Html.BeginForm("AddDb","htmlhelper",FormMethod.Post)){

    @Html.Label("name")
    @Html.TextBox("name")

    @Html.LabelFor(d=>d.DepartmentLocations[0].Location)
    @Html.EditorFor(d=> d.DepartmentLocations[0].Location)

    @Html.Label("empManager")
    @Html.DropDownList("empManager",ViewBag.empManager as SelectList,"choose one")

    <input type="submit" value="Add" />

}
