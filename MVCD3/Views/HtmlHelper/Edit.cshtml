@model Department

@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<h1 class="text-center text-primary">Edit Department</h1>

@using (Html.BeginForm("EditDb", "htmlhelper", FormMethod.Post))
{

    @Html.LabelFor(m=>m.Name,new {@class = "form-label"})
    @Html.TextBoxFor(m=>m.Name,new {@class = "form-control"})

    @Html.LabelFor(d => d.DepartmentLocations[0].Location, new {@class = "form-label"})
    @Html.EditorFor(d => d.DepartmentLocations[0].Location, new {@class = "form-label"})

    @Html.Label("empManager")
    @Html.DropDownList("empManager",ViewBag.empManager as SelectList,"choose one")

    <input type="submit" value="Edit" />
}

